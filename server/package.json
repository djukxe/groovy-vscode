{
  "name": "groovy-language-server",
  "version": "0.1.0",
  "description": "Language Server for Groovy",
  "main": "./dist/server.js",
  "scripts": {
    "compile": "tsc -b",
    "build": "npm run compile && webpack --mode production",
    "watch": "tsc -b -w",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "vscode-languageserver": "^9.0.1",
    "vscode-languageserver-textdocument": "^1.0.11"
  },
  "devDependencies": {
    "@types/jest": "^29.5.8",
    "@types/node": "^20.10.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "ts-loader": "^9.5.4",
    "typescript": "^5.3.0",
    "webpack": "^5.104.1",
    "webpack-cli": "^6.0.1"
  }
}
