{
  "name": "groovy-vscode-extension",
  "version": "0.1.0",
  "description": "Groovy Language Server and VSCode Extension",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "compile": "npm run compile --workspaces",
    "watch": "npm run watch --workspaces",
    "vscode:prepublish": "npm run compile"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
